<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
<div>
    <pre>
    <a href="http://x.app.loc/x.php">{{ linkX }}</a>
    </pre>
</div>
<div>
    <form action="{{ urlController }}" method="post" enctype="multipart/form-data">
    <fieldset>
        <legend>{{ formLegend }}</legend>
        <label>{{ loginTitle}}</label>
        <label>
            <input type="text" name="login" placeholder="">
        </label>
        <label>{{passwordTitle}}</label>
        <label>
            <input type="password" name="password" placeholder="">
        </label>
        <button name="submit">{{buttonTitle}}</button>
    </fieldset>
    </form>
</div>

</body>
</html>